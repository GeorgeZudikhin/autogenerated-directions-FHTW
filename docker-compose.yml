version: '3.8'
services:
  db:
    image: postgres:13
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: django_db
    ports:
      - "4000:5432"  
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
